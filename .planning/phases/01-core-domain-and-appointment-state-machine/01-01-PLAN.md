---
phase: 01-core-domain-and-appointment-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/core/config.py
  - backend/app/core/database.py
  - backend/app/core/security.py
  - backend/app/core/redis.py
  - backend/app/models/user.py
  - backend/app/models/appointment.py
  - backend/app/models/consent.py
  - backend/app/models/audit.py
  - backend/app/schemas/user.py
  - backend/app/schemas/appointment.py
  - backend/app/services/identity_service.py
  - backend/app/services/appointment_state.py
  - backend/tests/test_appointment_lifecycle.py
  - backend/tests/test_identity_foundation.py
autonomous: true
delegate_subagent:
  name: cursor
  model: composer-2.5 / claude-opus-5-thinking-high
  rationale: "The 8-state appointment lifecycle and foundational Unified Identity system form the transactional and architectural anchor for the entire platform. State transition correctness, JWT security infrastructure, and exclusion constraint templates require formal verification."
requirements:
  - PAT-01
  - PAT-04
  - DOC-01
  - DOC-03
  - TEL-01
  - RUL-01
  - RUL-03
  - RUL-04
  - CMP-02
must_haves:
  truths:
    - "Unified User and Identity model supports roles: patient, doctor, verification_reviewer, super_admin."
    - "JWT token generator signs and verifies access tokens with user_id, role, and expiration."
    - "Appointment model implements the 8 PRD Section 2A states: Requested, Confirmed, Rescheduled, CheckedIn, InConsultation, Completed, Cancelled, NoShow."
    - "Only valid appointment state transitions are permitted; invalid transitions raise HTTP 422 Unprocessable Entity."
    - "Redis distributed lock helper supports acquiring and releasing slot locks with TTL."
    - "Base entities for DPDP Act auditable consent and immutable audit logging are established."
  artifacts:
    - "backend/app/models/user.py and identity_service.py providing core auth infrastructure"
    - "backend/app/models/appointment.py defining Appointment, AppointmentMode, and AppointmentStatus enums"
    - "backend/app/services/appointment_state.py containing state transition matrix and guard decorators"
    - "backend/app/core/redis.py providing distributed lock manager"
  key_links:
    - "identity_service.py -> user.py -> database.py"
    - "appointment_state.py -> appointment.py -> database.py"
---

<objective>
Build the core foundational Python/FastAPI modular monolith backend, database connection engine, the Unified Identity and Authentication engine (User model, RBAC roles, JWT signing/verification, OTP primitives), and the 8-state Appointment Lifecycle state machine, along with Redis distributed locking primitives and the immutable audit log foundation.

Purpose: Establishing unified Identity in Phase 01 provides the shared authentication backbone for Doctor Onboarding (Phase 02) and Patient Discovery (Phase 03), while the appointment state machine serves as the transactional anchor for all downstream modules.
Output: Production-grade FastAPI project scaffold, PostgreSQL models, Unified Identity service, state transition engine, Redis distributed lock manager, and comprehensive unit tests.
</objective>

<tasks>
<task type="scaffold">
  <files>backend/app/core/config.py, backend/app/core/database.py, backend/app/core/security.py, backend/app/core/redis.py</files>
  <action>
    Configure async SQLAlchemy 2.0 engine for PostgreSQL, Redis connection pool for caching and locking, JWT signing and cryptographic utilities, and Pydantic Settings for environment configuration.
  </action>
  <verify>pytest backend/tests/test_core_setup.py || true</verify>
</task>

<task type="identity">
  <files>backend/app/models/user.py, backend/app/schemas/user.py, backend/app/services/identity_service.py</files>
  <action>
    1. Define User model:
       - id: UUID
       - phone_number: String (unique, indexed)
       - email: Optional[String]
       - role: Enum ('patient', 'doctor', 'verification_reviewer', 'super_admin')
       - is_active: Boolean
       - created_at, updated_at: DateTime
    2. Implement `IdentityService`:
       - `create_access_token(user_id, role, expires_delta)`: generates signed JWT.
       - `verify_token(token)`: decodes and validates JWT claims.
       - Passwordless/OTP code generation and verification primitives backed by Redis TTL.
  </action>
  <verify>pytest backend/tests/test_identity_foundation.py || true</verify>
</task>

<task type="model">
  <files>backend/app/models/appointment.py, backend/app/models/consent.py, backend/app/models/audit.py</files>
  <action>
    1. Define Appointment model with:
       - id: UUID
       - patient_id: UUID (ForeignKey to users)
       - doctor_id: UUID (ForeignKey to users)
       - clinic_id: UUID
       - mode: Enum ('in_person', 'video')
       - status: Enum ('requested', 'confirmed', 'rescheduled', 'checked_in', 'in_consultation', 'completed', 'cancelled', 'no_show')
       - slot_start: DateTime (timezone-aware)
       - slot_end: DateTime (timezone-aware)
       - fee_amount: Decimal
       - payment_status: Enum ('pending', 'captured', 'refunded', 'failed')
       - cancellation_reason: Optional[String]
       - created_at, updated_at: DateTime
    2. Add PostgreSQL exclusion constraint template for (doctor_id, slot_start, slot_end) ensuring zero slot overlap.
    3. Define DPDP ConsentRecord model (user_id, purpose, consent_version, granted_at, ip_address).
    4. Define AuditLog model (admin_user_id, target_entity_type, target_entity_id, action, previous_state, new_state, reason, ip_address, created_at).
  </action>
  <verify>alembic check || true</verify>
</task>

<task type="logic">
  <files>backend/app/schemas/appointment.py, backend/app/services/appointment_state.py</files>
  <action>
    Implement strict state machine transition matrix:
    - requested -> confirmed, cancelled
    - confirmed -> rescheduled, checked_in, cancelled, no_show
    - rescheduled -> confirmed, cancelled
    - checked_in -> in_consultation, no_show, cancelled
    - in_consultation -> completed
    - completed -> [TERMINAL]
    - cancelled -> [TERMINAL]
    - no_show -> [TERMINAL]
    Provide transition helper with validation hooks, event emission hooks (for Redis Pub/Sub), and descriptive error messages for disallowed transitions.
  </action>
  <verify>pytest backend/tests/test_appointment_lifecycle.py</verify>
</task>

<task type="test">
  <files>backend/tests/test_appointment_lifecycle.py, backend/tests/test_identity_foundation.py</files>
  <action>
    Write comprehensive test suite verifying:
    - User creation, JWT issuance, and claim extraction for all 4 roles.
    - All valid 8 appointment transitions succeed and update timestamps.
    - Invalid transitions raise validation exceptions.
    - Concurrency lock tests using Redis mock.
  </action>
  <verify>pytest backend/tests/test_appointment_lifecycle.py backend/tests/test_identity_foundation.py -v</verify>
</task>
</tasks>

<verification>
Run `pytest backend/tests/` to ensure 100% test pass rate for Identity tokens, state machine transitions, and concurrency primitives.
</verification>

<success_criteria>
- [ ] Unified Identity system established; issues valid JWTs for all roles.
- [ ] Appointment state machine supports all 8 states specified in PRD Section 2A.
- [ ] Direct illegal state leaps are impossible and strictly guarded.
- [ ] Redis lock manager provides atomic acquire/release semantics.
- [ ] Reusable DPDP consent and audit log models are established.
</success_criteria>
