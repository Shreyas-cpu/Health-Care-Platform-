---
phase: 08-admin-ops-moderation-and-audit-logging
plan: 01
type: execute
wave: 5
depends_on:
  - 01-core-domain-and-appointment-state-machine
  - 02-doctor-onboarding-and-verification-pipeline
  - 03-patient-auth-catalog-and-search
  - 07-clinical-ops-queue-and-prescription-engine
files_modified:
  - backend/app/models/review.py
  - backend/app/models/notification.py
  - backend/app/schemas/admin.py
  - backend/app/schemas/review.py
  - backend/app/services/review_service.py
  - backend/app/services/admin_analytics.py
  - backend/app/services/reminder_service.py
  - backend/app/api/v1/reviews.py
  - backend/app/api/v1/admin_dashboard.py
  - backend/app/api/v1/admin_moderation.py
  - backend/tests/test_admin_audit_and_reviews.py
autonomous: true
delegate_subagent:
  name: cursor
  model: claude-opus-5-thinking-high / gpt-5.3-codex
  rationale: "Admin boundaries and audit log immutability are high-security areas. Requires strict RBAC enforcement (super_admin vs verification_reviewer) and verifiable audit trails for content moderation and revenue metrics."
requirements:
  - PAT-05
  - PAT-07
  - ADM-02
  - ADM-03
  - ADM-04
  - RUL-04
must_haves:
  truths:
    - "RBAC strictly separates super_admin (full access) from verification_reviewer (restricted to verification queue)."
    - "Patient can submit exactly one 1-5 star rating and optional written review after an appointment is Completed."
    - "Doctors' aggregate rating automatically updates when an approved review is posted."
    - "Admin moderation interface allows hiding or removing reviews with mandatory audit logging."
    - "Every administrative action (review moderation, doctor status change, manual payment adjustment) writes an immutable audit record."
    - "Single appointment reminder dispatches automatically before scheduled slot."
    - "Platform dashboard aggregates total doctors, total bookings, and gross transaction value."
  artifacts:
    - "backend/app/models/review.py and schemas/review.py"
    - "backend/app/services/review_service.py managing submission and moderation"
    - "backend/app/api/v1/admin_dashboard.py exposing high-level platform telemetry"
    - "backend/app/api/v1/admin_moderation.py managing review content moderation"
  key_links:
    - "reviews.py -> review_service.py -> audit.py -> database.py"
---

<objective>
Implement the administrative oversight console, RBAC enforcement, post-visit rating/review lifecycle, review moderation tooling, single automated reminder dispatch, and platform telemetry—verifying immutable audit logging across all administrative actions.

Purpose: Maintain marketplace quality, trust, and safety through administrative oversight, verified patient reviews, and tamper-proof governance trails.
Output: Admin dashboard APIs, review submission and moderation endpoints, automated reminder tasks, RBAC middleware, and audit trail verification tests.
</objective>

<tasks>
<task type="rbac">
  <files>backend/app/schemas/admin.py, backend/app/api/v1/admin_dashboard.py</files>
  <action>
    1. Implement RBAC dependency guards:
       - `require_super_admin`: permits only `super_admin` role.
       - `require_verification_reviewer`: permits `verification_reviewer` and `super_admin`.
    2. Implement `GET /api/v1/admin/dashboard/telemetry`:
       - Calculates: Total Verified Doctors, Total Confirmed Bookings, Total Completed Consultations, Gross Transaction Value (GTV).
       - Guarded with `require_super_admin`.
  </action>
  <verify>pytest backend/tests/test_admin_audit_and_reviews.py -k "test_rbac" || true</verify>
</task>

<task type="reviews">
  <files>backend/app/models/review.py, backend/app/schemas/review.py, backend/app/services/review_service.py, backend/app/api/v1/reviews.py</files>
  <action>
    1. Define Review model:
       - id: UUID, appointment_id: UUID (unique), doctor_id: UUID, patient_id: UUID
       - rating: Integer (1 to 5), review_text: Optional[Text]
       - status: Enum ('published', 'flagged', 'hidden', 'removed')
       - created_at: DateTime
    2. Implement `POST /api/v1/reviews/`:
       - Precondition: Appointment must be in `completed` state.
       - Exactly one review allowed per completed appointment.
       - Updates doctor's aggregate star rating and review count.
  </action>
  <verify>pytest backend/tests/test_admin_audit_and_reviews.py -k "test_review_submission" || true</verify>
</task>

<task type="moderation">
  <files>backend/app/api/v1/admin_moderation.py, backend/app/services/audit.py</files>
  <action>
    1. Implement `GET /api/v1/admin/moderation/reviews` (list flagged or recent reviews).
    2. Implement `POST /api/v1/admin/moderation/reviews/{review_id}/action`:
       - Action: 'hide' | 'remove' | 'approve'
       - Mandatory field: `reason_text`
       - Gating Hard Rule (RUL-04): Writes to `audit_logs` inside transaction with admin_id, action, previous_status, new_status, and reason.
       - Recalculates doctor aggregate rating if a review is hidden or removed.
  </action>
  <verify>pytest backend/tests/test_admin_audit_and_reviews.py -k "test_review_moderation"</verify>
</task>

<task type="notifications">
  <files>backend/app/models/notification.py, backend/app/services/reminder_service.py</files>
  <action>
    1. Implement single pre-appointment reminder Celery periodic task:
       - Scans for appointments scheduled in next 2 hours.
       - Checks `reminder_sent == False`.
       - Dispatches SMS/Email reminder with join/arrival instructions and marks `reminder_sent = True`.
  </action>
  <verify>pytest backend/tests/test_admin_audit_and_reviews.py -k "test_reminder" || true</verify>
</task>

<task type="test">
  <files>backend/tests/test_admin_audit_and_reviews.py</files>
  <action>
    Write tests verifying:
    - Reviewer role blocked from dashboard telemetry (HTTP 403); super_admin permitted.
    - Patient cannot review uncompleted appointment.
    - Patient can review completed appointment once; duplicate attempt rejected with 409.
    - Admin hiding a review creates immutable audit log record (RUL-04) and adjusts doctor rating.
    - Appointment reminder dispatches exactly once.
  </action>
  <verify>pytest backend/tests/test_admin_audit_and_reviews.py -v</verify>
</task>
</tasks>

<verification>
Run `pytest backend/tests/test_admin_audit_and_reviews.py` to confirm RBAC security, review moderation, reminder dispatch, and immutable audit logging.
</verification>

<success_criteria>
- [ ] RBAC strictly isolates super_admin telemetry from verification reviewers.
- [ ] Post-visit rating and review prompt completes the patient feedback loop.
- [ ] Cross-cutting Hard Rule RUL-04 is verified: 100% of admin moderation actions write immutable audit logs.
- [ ] Single pre-visit reminder notification executes reliably.
</success_criteria>
