---
phase: 03-patient-auth-catalog-and-search
plan: 01
type: execute
wave: 2
depends_on:
  - 01-core-domain-and-appointment-state-machine
  - 02-doctor-onboarding-and-verification-pipeline
files_modified:
  - backend/app/models/patient.py
  - backend/app/models/clinic.py
  - backend/app/schemas/patient.py
  - backend/app/schemas/search.py
  - backend/app/services/search_index.py
  - backend/app/services/catalog.py
  - backend/app/api/v1/patient_auth.py
  - backend/app/api/v1/search.py
  - backend/app/api/v1/doctor_profile.py
  - backend/tests/test_search_sync.py
autonomous: true
delegate_subagent:
  name: codex
  model: gpt-5.6-terra
  rationale: "Specializing the Phase 01 Identity system for patient mobile OTP authentication, scaffolding doctor catalog endpoints, and implementing real-time search index synchronization."
requirements:
  - PAT-01
  - PAT-02
  - PAT-03
  - DOC-05
  - RUL-02
  - CMP-02
must_haves:
  truths:
    - "Patient authenticates using Indian mobile number and OTP via Phase 01 Identity system, receiving a valid JWT."
    - "DPDP Act consent is captured during registration and stored in the auditable consent table."
    - "Search returns only doctors who are both 'verified' and have 'listing_online == True'."
    - "When a doctor toggles listing_online or video_enabled, search cache is invalidated and updated in real time."
    - "Search filters operate on specialty, city/locality, fee range, gender, and availability today."
  artifacts:
    - "backend/app/api/v1/patient_auth.py exposing mobile OTP authentication for patients"
    - "backend/app/services/search_index.py handling doctor search queries and instant index sync"
    - "backend/app/api/v1/search.py exposing GET /api/v1/search/doctors"
  key_links:
    - "patient_auth.py -> identity_service.py -> patient.py -> database.py"
    - "search.py -> search_index.py -> catalog.py -> redis.py"
---

<objective>
Implement patient mobile OTP authentication (specializing Phase 01 Identity) with DPDP consent capture, public doctor profile endpoints, and the real-time synchronized doctor search catalog.

Purpose: Patients must be able to securely sign in and discover verified, currently available doctors with instant consistency when doctors toggle their availability.
Output: Patient OTP auth endpoints, doctor catalog APIs, search index integration with sub-200ms cache invalidation, and automated visibility propagation tests.
</objective>

<tasks>
<task type="auth">
  <files>backend/app/models/patient.py, backend/app/api/v1/patient_auth.py</files>
  <action>
    1. Define Patient model (user_id: UUID FK to users, full_name, gender, date_of_birth). Note: Primary self-profile only; linked dependents are deferred to Phase 2.
    2. Implement `POST /api/v1/auth/patient/send-otp` and `POST /api/v1/auth/patient/verify-otp` using Phase 01 `IdentityService`.
    3. Gating Check (CMP-02): Capture DPDP consent version, purpose ('patient_registration_and_care'), timestamp, and client IP in `consent_records` during verification.
  </action>
  <verify>pytest backend/tests/test_search_sync.py -k "test_patient_auth" || true</verify>
</task>

<task type="catalog">
  <files>backend/app/models/clinic.py, backend/app/services/catalog.py, backend/app/api/v1/doctor_profile.py</files>
  <action>
    1. Define single Clinic model per doctor (name, address, city, locality, pincode, contact_number).
    2. Add consultation fee fields to Doctor profile: `in_person_fee: Decimal`, `video_fee: Decimal`.
    3. Implement `GET /api/v1/doctors/{doctor_id}` returning public profile: bio, qualifications, specialty, clinic address, aggregate star rating, and fees.
    4. Implement doctor toggle endpoints: `POST /api/v1/doctors/me/toggle-listing` and `POST /api/v1/doctors/me/toggle-video`.
  </action>
  <verify>pytest backend/tests/test_search_sync.py -k "test_catalog" || true</verify>
</task>

<task type="search">
  <files>backend/app/services/search_index.py, backend/app/api/v1/search.py</files>
  <action>
    1. Implement search service backed by OpenSearch / PostgreSQL full-text index with Redis query cache:
       - Query params: `specialty`, `locality`, `city`, `min_fee`, `max_fee`, `gender`, `available_today`, `video_available`.
    2. Hard Rule (RUL-02): Enforce filter `verification_status == 'verified' AND listing_online == True`.
    3. When doctor toggles `listing_online` or `video_enabled`, hook emits cache invalidation key `cache:search:*` and pushes atomic update to index.
  </action>
  <verify>pytest backend/tests/test_search_sync.py</verify>
</task>

<task type="test">
  <files>backend/tests/test_search_sync.py</files>
  <action>
    Write test verifying:
    - Doctor verified + online -> appears in search results.
    - Doctor unverified -> excluded from search.
    - Doctor toggles offline -> immediately excluded from search results (< 200ms).
    - Doctor disables video -> immediately excluded from 'video_available' search filter.
    - DPDP consent recorded upon patient OTP verification.
  </action>
  <verify>pytest backend/tests/test_search_sync.py -v</verify>
</task>
</tasks>

<verification>
Run `pytest backend/tests/test_search_sync.py` to confirm that patient auth succeeds, DPDP consent is recorded, and doctor visibility toggles propagate to search in real time.
</verification>

<success_criteria>
- [ ] Patient authenticates with mobile OTP on Phase 01 Identity system and receives JWT.
- [ ] DPDP consent capture is stored in the audit table on registration.
- [ ] Hard Rule RUL-02 is verified: Doctor visibility toggles immediately reflect in search results.
- [ ] Doctor profile displays qualifications, clinic address, aggregate rating, and fixed fees.
</success_criteria>
