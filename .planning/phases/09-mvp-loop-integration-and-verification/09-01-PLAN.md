---
phase: 09-mvp-loop-integration-and-verification
plan: 01
type: execute
wave: 6
depends_on:
  - 01-core-domain-and-appointment-state-machine
  - 02-doctor-onboarding-and-verification-pipeline
  - 03-patient-auth-catalog-and-search
  - 04-scheduling-atomic-booking-and-payments
  - 05-realtime-gateway-websocket-signaling
  - 06-teleconsultation-engine-and-session-model
  - 07-clinical-ops-queue-and-prescription-engine
  - 08-admin-ops-moderation-and-audit-logging
files_modified:
  - backend/tests/e2e/test_mvp_golden_loop.py
  - backend/tests/stress/test_concurrency_hard_rules.py
autonomous: true
delegate_subagent:
  name: cursor
  model: claude-opus-5-thinking-high
  rationale: "Verifying high-stakes concurrency correctness, atomic rollback under gateway failures, and audit log immutability under race conditions requires deep reasoning about distributed locks and transactional ACID boundaries. Assigned to cursor, matching Phase 04 and Phase 02."
requirements:
  - PAT-01
  - PAT-02
  - PAT-03
  - PAT-04
  - PAT-05
  - PAT-06
  - PAT-07
  - PAT-08
  - PAT-09
  - DOC-01
  - DOC-02
  - DOC-03
  - DOC-04
  - DOC-05
  - TEL-01
  - TEL-02
  - TEL-03
  - ADM-01
  - ADM-02
  - ADM-03
  - ADM-04
  - RUL-01
  - RUL-02
  - RUL-03
  - RUL-04
must_haves:
  truths:
    - "No double-booking occurs under 100 concurrent slot acquisition attempts (RUL-01)."
    - "Doctor visibility toggle propagates to search within 200ms without stale appearances (RUL-02)."
    - "Atomic slot reservation, payment capture, and rollback execute cleanly without orphaned records (RUL-03)."
    - "100% of admin status changes, refunds, and review moderations produce immutable audit logs (RUL-04)."
    - "Full MVP golden loop executes from patient discovery to completed prescription and review with 0 manual interventions."
  artifacts:
    - "backend/tests/stress/test_concurrency_hard_rules.py validating RUL-01..RUL-04 under race conditions"
    - "backend/tests/e2e/test_mvp_golden_loop.py running full end-to-end integration flow"
  key_links:
    - "test_concurrency_hard_rules.py -> booking_service.py -> payment_gateway.py -> audit.py"
---

<objective>
Execute deep concurrency stress testing, race condition validation on slots and payments, and automated end-to-end verification of the full MVP loop across modules, validating all 4 cross-cutting hard rules (RUL-01 to RUL-04).

Purpose: High-stakes transactional correctness verification. Guarantee that the marketplace is impervious to double-booking, race conditions, split-brain payments, and audit evasion.
Output: Automated concurrency test suite, end-to-end golden loop test runner, and passing verification reports.
</objective>

<tasks>
<task type="stress-test">
  <files>backend/tests/stress/test_concurrency_hard_rules.py</files>
  <action>
    Implement concurrency stress suite for RUL-01 through RUL-04:
    1. RUL-01: Spin up 100 concurrent async threads attempting to book the exact same slot. Verify exactly 1 receives Confirmed status; 99 receive 409 Conflict. Inspect DB exclusion constraints.
    2. RUL-02: Hammer search endpoint with continuous queries while toggling doctor `listing_online` from True to False. Verify zero responses post-invalidation contain the doctor (< 200ms latency).
    3. RUL-03: Simulate dropped Razorpay webhook and payment gateway failure mid-flight. Verify slot lock expires within TTL and appointment rolls back to `cancelled` with zero orphaned charges. Test cancellation engine automated refund.
    4. RUL-04: Execute 50 concurrent admin status mutations across verification, reviews, and refunds. Verify exactly 50 tamper-proof rows exist in `audit_logs` matching admin ID, entity, diff, and reason.
  </action>
  <verify>pytest backend/tests/stress/test_concurrency_hard_rules.py -v</verify>
</task>

<task type="e2e-test">
  <files>backend/tests/e2e/test_mvp_golden_loop.py</files>
  <action>
    Implement end-to-end integration test exercising the complete golden loop:
    1. Doctor registers using Phase 01 Identity and uploads verification docs (DOC-01).
    2. Reviewer inspects docs and promotes doctor to `verified` (ADM-01, RUL-04).
    3. Doctor sets recurring weekly schedule and toggles listing online (DOC-02, DOC-05).
    4. Patient authenticates via mobile OTP with DPDP consent capture (PAT-01, CMP-02).
    5. Patient searches for doctor, views profile, and reserves slot (PAT-02, PAT-03, PAT-04).
    6. Razorpay webhook confirms payment; slot transitions to `confirmed` (PAT-08, RUL-03).
    7. Automated reminder notification dispatches (PAT-05).
    8. Patient enters waiting room -> `checked_in`; doctor starts session -> `in_consultation` (TEL-01, TEL-03).
    9. In-call chat messages persist to database (TEL-02).
    10. Doctor completes session -> `completed` (DOC-03).
    11. Doctor writes structured prescription; Celery compiles signed PDF (DOC-04, PAT-06).
    12. Patient views and downloads PDF prescription from health records (PAT-06).
    13. Patient submits 5-star rating and written review (PAT-07).
    14. Admin audits platform metrics and review queue (ADM-03, ADM-04, RUL-04).
  </action>
  <verify>pytest backend/tests/e2e/test_mvp_golden_loop.py -v</verify>
</task>
</tasks>

<verification>
Run `pytest backend/tests/stress/` and `pytest backend/tests/e2e/` to confirm 100% test pass rate across all concurrency race conditions and end-to-end flows.
</verification>

<success_criteria>
- [ ] Cross-cutting Hard Rules RUL-01 through RUL-04 verified under heavy concurrency.
- [ ] Complete MVP golden loop runs successfully from discovery to prescription download.
- [ ] Zero race conditions or orphaned financial records detected.
</success_criteria>
