---
phase: 09-mvp-loop-integration-and-verification
plan: 02
type: execute
wave: 6
depends_on:
  - 09-mvp-loop-integration-and-verification
files_modified:
  - backend/app/main.py
  - docs/OPENAPI_SPEC.json
  - docs/COMPLIANCE_SIGN_OFF.md
autonomous: true
delegate_subagent:
  name: devin
  model: SWE-1.6 Slow
  rationale: "Devin (SWE-1.6 Slow) is ideally suited for system-wide code cleanup, formatting standardization, docstring coverage passes, OpenAPI contract validation, and compliance checklist documentation."
requirements:
  - CMP-01
  - CMP-02
must_haves:
  truths:
    - "All public API routes and state machine transition methods possess comprehensive docstrings."
    - "Code formatting passes 100% with Black, Ruff, and Prettier."
    - "Production OpenAPI v3 specification is exported and validated."
    - "Formal compliance sign-off document exists for CMP-01 and CMP-02."
    - "Explicit audit confirms zero leakage of any of the 16 deferred Phase 2 items."
  artifacts:
    - "docs/OPENAPI_SPEC.json containing complete exported API contracts"
    - "docs/COMPLIANCE_SIGN_OFF.md documenting legal sign-off gates"
  key_links:
    - "main.py -> docs/OPENAPI_SPEC.json"
---

<objective>
Execute codebase-wide cleanup, format code, verify docstring coverage, export the OpenAPI v3 contract, compile the compliance legal sign-off checklist, and verify strict exclusion of all 16 Phase 2 deferred features.

Purpose: Polish and stabilize the platform codebase, ensure crystal-clear API documentation for mobile and web clients, and formalize legal compliance sign-off requirements before production launch.
Output: Cleaned codebase, exported OpenAPI spec file, compliance sign-off document, and Phase 2 boundary audit.
</objective>

<tasks>
<task type="cleanup">
  <files>backend/app/main.py</files>
  <action>
    1. Run code formatting: `black backend/` and `ruff check --fix backend/`.
    2. Ensure comprehensive docstring coverage across all routers, services, and models.
    3. Verify TypeScript formatting and linting in `realtime-gateway/`.
  </action>
  <verify>black --check backend/ && ruff check backend/</verify>
</task>

<task type="openapi">
  <files>docs/OPENAPI_SPEC.json</files>
  <action>
    Export FastAPI OpenAPI schema to `docs/OPENAPI_SPEC.json` and validate schema syntax against OpenAPI 3.1.0 specifications.
  </action>
  <verify>python -c "import json; spec = json.load(open('docs/OPENAPI_SPEC.json')); assert 'paths' in spec"</verify>
</task>

<task type="compliance-gate">
  <files>docs/COMPLIANCE_SIGN_OFF.md</files>
  <action>
    Author `docs/COMPLIANCE_SIGN_OFF.md` detailing:
    1. CMP-01: Telemedicine Restricted Drugs checklist (Schedule X, narcotics, first-visit vs follow-up prescribing limits) awaiting legal counsel sign-off.
    2. CMP-02: DPDP Act patient consent capture, data retention schedule, and encryption at rest in AWS ap-south-1.
    3. Phase 2 Scope Audit: Line-by-line verification certifying that none of the 16 explicitly deferred items are active in Phase 1.
  </action>
  <verify>test -f docs/COMPLIANCE_SIGN_OFF.md</verify>
</task>
</tasks>

<verification>
Verify that `docs/OPENAPI_SPEC.json` and `docs/COMPLIANCE_SIGN_OFF.md` are generated and that all lint/format checks succeed.
</verification>

<success_criteria>
- [ ] Codebase passes all linter and formatting checks cleanly.
- [ ] Complete OpenAPI v3 specification exported and validated.
- [ ] Compliance sign-off documentation formalized for legal review.
- [ ] Scope boundary audit certifies zero Phase 2 feature leakage.
</success_criteria>
